{
  "name": "gumdaeri",
  "version": "1.0.2",
  "private": true,
  "description": "브라우저에서 동작하는 Office 원고 검수 도구",
  "scripts": {
    "build": "bash build.sh",
    "test": "bash build.sh && node test/run.js",
    "verify": "npm test"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  },
  "engines": {
    "node": ">=18"
  }
}
